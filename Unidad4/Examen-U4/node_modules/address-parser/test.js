var addP = require('./index');
var parser = new addP();
var list = ['via di campo marzio', 'roma mar', '20 settem', 'appia nuova km 11250'];
for(var i =0; i<list.length; i++) {
    console.log(list[i], parser.parse(list[i]));
}